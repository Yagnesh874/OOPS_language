#include <iostream>
using namespace std;

int main()
{
    float length, width, area;
    cout << "enter the length value ";
    cin >> length;
    cout << "enter the width value ";
    cin >> width;
    area = length * width;
    cout << "This is the area of a number " << area;
}